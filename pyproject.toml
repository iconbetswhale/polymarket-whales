[project]
name = "iconbets-polymarket-wallet-tracker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "flask",
  "requests",
  "gunicorn",
  "python-dotenv",
  "psycopg[binary]>=3.2,<4",
]

[tool.vercel]
entrypoint = "app:app"
